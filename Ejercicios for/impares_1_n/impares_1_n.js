let n = parseInt(prompt("Ingrese el numero hasta el cual quiere sabe rlos numeros impares"))
for (let index = 0; index < n; index++) {
    if (index % 2 != 0) {
        console.log(index);
        }
    
}